{
  "name": "shift-deck",
  "private": true,
  "description": "Tests only. The app itself has no dependencies and no build step — the files in the repo root are what gets served.",
  "scripts": {
    "test": "node --test tests/parser.test.js",
    "test:update": "UPDATE=1 node --test tests/parser.test.js"
  }
}
